<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/header') %>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/css/style.css">
</head>

<body class="d-flex flex-column min-vh-100">

    <div class="container mt-4">
        <form action="/add" method="POST" class="row g-3 align-items-center">
            <div class="col-md-6">
                <input type="text" name="title" class="form-control" placeholder="Add a task" required>
            </div>
            <div class="col-md-3">
                <select name="priority" class="form-select">
                    <option value="Low">Low</option>
                    <option value="Medium">Medium</option>
                    <option value="High">High</option>
                </select>
            </div>
            <div class="col-md-3">
                <button class="btn btn-success w-100">Add</button>
            </div>
        </form>

        <ul class="list-group mt-4">
            <% todos.forEach(todo=> { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <% if (editingId===String(todo._id)) { %>
                        <form action="/edit/<%= todo._id %>" method="POST"
                            class="w-100 d-flex flex-wrap gap-2 align-items-center">
                            <input type="text" name="title" value="<%= todo.title %>" class="form-control w-25"
                                required>
                            <select name="priority" class="form-select w-25">
                                <option value="Low" <%=todo.priority==='Low' ? 'selected' : '' %>>Low</option>
                                <option value="Medium" <%=todo.priority==='Medium' ? 'selected' : '' %>>Medium</option>
                                <option value="High" <%=todo.priority==='High' ? 'selected' : '' %>>High</option>
                            </select>
                            <button type="submit" class="btn btn-success">Save</button>
                            <a href="/" class="btn btn-secondary">Cancel</a>
                        </form>
                        <% } else { %>
                            <span>
                                <strong>
                                    <%= todo.title %>
                                </strong> —
                                <span class="badge bg-info text-dark">
                                    <%= todo.priority %>
                                </span>
                            </span>
                            <div class="d-flex gap-2">
                                <a href="/edit/<%= todo._id %>" class="btn btn-warning">Edit</a>
                                <form action="/delete/<%= todo._id %>" method="POST">
                                    <button class="btn btn-danger">Delete</button>
                                </form>
                            </div>
                            <% } %>
                </li>
                <% }) %>
        </ul>
    </div>

    <%- include('partials/footer') %>
        <script src="/js/main.js"></script>
</body>

</html>